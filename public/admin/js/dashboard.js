/* Page controller marker for the requested admin file structure. page.js owns shared loading so all pages keep one auth and error contract. */
window.AdminPageModules = window.AdminPageModules || {};
window.AdminPageModules.dashboard = true;
