'use client';

import { useEffect, useState } from 'react';
import { Eye, MessageCircle, TrendingUp } from 'lucide-react';
import { getSellerStats, type SellerStats } from '@/lib/actions/analytics';
import { MiniChart } from './MiniChart';
export function SellerAnalyticsCard({ sellerId }: { sellerId: string }) { const [stats, setStats] = useState<SellerStats | null>(null); const [days, setDays] = useState<7 | 30>(7); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { setLoading(true); getSellerStats(sellerId, days).then(result => { if (result.ok) { setStats(result.data); setError(''); } else setError(result.error || 'Analytics belum tersedia.'); setLoading(false); }); }, [sellerId, days]); if (loading) return <div className="analytics-card loading-card" aria-busy="true">Memuat performa toko...</div>; if (error) return <div className="analytics-card analytics-error" role="status">{error}</div>; if (!stats) return null; return <section className="analytics-card"><header className="analytics-head"><div><span className="eyebrow">Performa seller</span><h3>Ringkasan toko</h3></div><select value={days} onChange={event => setDays(Number(event.target.value) as 7 | 30)} aria-label="Rentang analytics"><option value="7">7 hari</option><option value="30">30 hari</option></select></header><div className="analytics-metrics"><div><Eye size={17}/><strong>{stats.totalViews}</strong><small>Total views</small></div><div><MessageCircle size={17}/><strong>{stats.totalContacts}</strong><small>Pesan masuk</small></div><div><TrendingUp size={17}/><strong>{stats.responseRate}%</strong><small>Tingkat respon</small></div></div><MiniChart values={stats.daily.map(item => item.views + item.contacts)} labels={stats.daily.map(item => item.date.slice(5))}/></section>; }
