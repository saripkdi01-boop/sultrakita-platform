'use client';

import { SidebarDesktop } from './SidebarDesktop';

/** Desktop navigation rail retained as a dedicated Facebook-style layout slot. */
export function LeftSidebar() {
  return <SidebarDesktop />;
}
