'use client';

import { FormEvent, useEffect, useRef, useState } from 'react';
import { Bell, ChevronLeft, ChevronRight, CircleHelp, LogOut, Palette, Shield, Store, UserRound, X } from 'lucide-react';
import { SettingTab, useProfileStore, UserProfile, UserRole } from '@/store/profile';

const categories: { id: SettingTab; label: string; description: string; icon: typeof UserRound }[] = [
  { id: 'account', label: 'Akun dan profil', description: 'Nama, kontak, bio, dan identitas lokal', icon: UserRound },
  { id: 'privacy', label: 'Privasi', description: 'Kendalikan siapa yang dapat melihat aktivitasmu', icon: Shield },
  { id: 'notifications', label: 'Notifikasi', description: 'Atur kabar dari aktivitas dan komunitas', icon: Bell },
  { id: 'appearance', label: 'Tampilan', description: 'Tema, video, bahasa, dan aksesibilitas', icon: Palette },
  { id: 'seller', label: 'Seller dan toko', description: 'Pengaturan jualan dan operasional toko', icon: Store },
  { id: 'help', label: 'Bantuan dan feedback', description: 'Pusat bantuan dan laporan masalah', icon: CircleHelp },
];
const interests = ['Marketplace', 'Kuliner', 'Wisata', 'UMKM', 'Motor', 'Properti', 'Fashion', 'Hobi'];

export function ProfileHub() {
  const { profile, menuOpen, setupOpen, settingsOpen, activeTab, toggleMenu, openSetup, openSettings, closeOverlays } = useProfileStore();
  const triggerRef = useRef<HTMLButtonElement>(null);
  useEffect(() => { const onKey = (event: KeyboardEvent) => { if (event.key === 'Escape') closeOverlays(); }; document.addEventListener('keydown', onKey); return () => document.removeEventListener('keydown', onKey); }, [closeOverlays]);
  useEffect(() => { document.documentElement.classList.toggle('dark', profile.dark_mode); document.documentElement.classList.toggle('reduce-motion', profile.reduce_motion); }, [profile.dark_mode, profile.reduce_motion]);
  return <>
    <div className="profile-hub-anchor"><button ref={triggerRef} className="profile-pill profile-hub-trigger" onClick={toggleMenu} aria-expanded={menuOpen} aria-controls="profile-menu" aria-label="Buka menu profil"><span className="avatar">{profile.avatar_url ? <img src={profile.avatar_url} alt="Profil"/> : profile.full_name.slice(0, 2).toUpperCase()}</span><span className="profile-name">{profile.full_name.split(' ')[0]}</span></button>{menuOpen && <ProfileMenu onClose={closeOverlays} onSetup={openSetup} onSettings={openSettings}/>}</div>
    {setupOpen && <ProfileSetup onClose={closeOverlays}/>} {settingsOpen && <SettingsPanel onClose={closeOverlays} activeTab={activeTab}/>} 
  </>;
}

function ProfileMenu({ onClose, onSetup, onSettings }: { onClose: () => void; onSetup: () => void; onSettings: (tab?: SettingTab) => void }) { const { profile } = useProfileStore(); return <div id="profile-menu" className="profile-menu-panel shadow-dropdown border border-sultra-mint dark:border-sultra-forest/30" role="menu"><div className="profile-menu-head"><div className="avatar large">{profile.full_name.slice(0, 2).toUpperCase()}</div><div><strong>{profile.full_name}</strong><small>{profile.email || 'Lengkapi profilmu'}</small><span>{profile.role === 'seller' ? 'Seller' : 'Buyer'} · {profile.city}</span></div></div><button role="menuitem" onClick={onSetup}><UserRound size={17}/> Setup profil <ChevronRight size={15}/></button><button role="menuitem" onClick={() => onSettings('account')}><UserRound size={17}/> Account Center <ChevronRight size={15}/></button><button role="menuitem" onClick={() => onSettings('privacy')}><Shield size={17}/> Privasi <ChevronRight size={15}/></button><button role="menuitem" onClick={() => onSettings('notifications')}><Bell size={17}/> Notifikasi <ChevronRight size={15}/></button><button role="menuitem" onClick={() => onSettings('appearance')}><Palette size={17}/> Preferensi tampilan <ChevronRight size={15}/></button><button role="menuitem" onClick={() => onSettings('seller')}><Store size={17}/> Pengaturan seller <ChevronRight size={15}/></button><button role="menuitem" onClick={() => onSettings('help')}><CircleHelp size={17}/> Bantuan & feedback <ChevronRight size={15}/></button><div className="profile-menu-separator"/><button className="danger-menu" role="menuitem" onClick={() => { onClose(); window.dispatchEvent(new CustomEvent('sultra-toast', { detail: 'Sesi demo ditutup.' })); }}><LogOut size={17}/> Keluar</button></div> }

function ProfileSetup({ onClose }: { onClose: () => void }) { const { profile, setProfile } = useProfileStore(); const [step, setStep] = useState(1); const [draft, setDraft] = useState(profile); const update = (key: keyof typeof draft, value: string) => setDraft({ ...draft, [key]: value }); const finish = (event: FormEvent) => { event.preventDefault(); setProfile(draft); onClose(); window.dispatchEvent(new CustomEvent('sultra-toast', { detail: 'Profil berhasil disimpan.' })); }; return <div className="profile-overlay"><form className="profile-dialog setup-dialog" onSubmit={finish}><header><div><span className="eyebrow">Setup profil</span><h2>Kenalkan dirimu ke warga</h2><small>Langkah {step} dari 4</small></div><button type="button" onClick={onClose} aria-label="Tutup setup profil"><X size={19}/></button></header><div className="setup-progress"><i style={{ width: `${step * 25}%` }}/></div><div className="setup-content">{step === 1 && <><label>Nama lengkap<input value={draft.full_name} onChange={e => update('full_name', e.target.value)} required/></label><label>Username<input value={draft.username} onChange={e => update('username', e.target.value)} placeholder="contoh: maya_kendari"/></label><label>Bio singkat<textarea value={draft.bio} onChange={e => update('bio', e.target.value)} rows={3} placeholder="Ceritakan sedikit tentang dirimu"/></label></>}{step === 2 && <><label>Kota/kabupaten<input value={draft.city} onChange={e => update('city', e.target.value)}/></label><label>Distrik<input value={draft.district} onChange={e => update('district', e.target.value)}/></label><label>Bahasa utama<select value={draft.language} onChange={e => update('language', e.target.value)}><option>Bahasa Indonesia</option><option>Indonesia dan bahasa daerah</option></select></label></>}{step === 3 && <div><strong>Minat komunitas</strong><p className="muted-copy">Pilih topik agar feed SultraKita lebih relevan.</p><div className="interest-grid">{interests.map(item => <button type="button" key={item} className={draft.interests.includes(item) ? 'selected' : ''} onClick={() => setDraft({ ...draft, interests: draft.interests.includes(item) ? draft.interests.filter(x => x !== item) : [...draft.interests, item] })}>{item}</button>)}</div></div>}{step === 4 && <div><strong>Saya bergabung sebagai</strong><div className="role-grid">{([['buyer', 'Pembeli'], ['seller', 'Seller / UMKM'], ['creator', 'Kreator konten'], ['community', 'Organisasi komunitas']] as [UserRole, string][]).map(([value, label]) => <button type="button" key={value} className={draft.role === value ? 'selected' : ''} onClick={() => setDraft({ ...draft, role: value })}><b>{label}</b><small>{value === 'seller' ? 'Jual produk atau jasa lokal' : 'Bangun pengalaman warga'}</small></button>)}</div></div>}</div><footer><button type="button" className="text-link" onClick={onClose}>Simpan nanti</button><div>{step > 1 && <button type="button" className="soft-btn" onClick={() => setStep(step - 1)}><ChevronLeft size={15}/> Kembali</button>}<button type={step === 4 ? 'submit' : 'button'} className="primary-btn" onClick={() => step < 4 && setStep(step + 1)}>{step === 4 ? 'Selesai' : 'Lanjut'} <ChevronRight size={15}/></button></div></footer></form></div> }

function SettingsPanel({ onClose, activeTab }: { onClose: () => void; activeTab: SettingTab }) { const [mobileDetail, setMobileDetail] = useState(false); const active = categories.find(item => item.id === activeTab) || categories[0]; return <div className="profile-overlay"><div className="profile-dialog settings-dialog"><header><div><span className="eyebrow">Account Center</span><h2>Pengaturan SultraKita</h2></div><button onClick={onClose} aria-label="Tutup pengaturan"><X size={19}/></button></header><div className="settings-layout"><nav className={mobileDetail ? 'settings-nav mobile-hidden' : 'settings-nav'}>{categories.map(item => <button key={item.id} className={`${item.id === activeTab ? 'active' : ''} ${item.id === activeTab ? 'bg-sultra-teal text-white shadow-soft' : ''}`} onClick={() => { useProfileStore.getState().setActiveTab(item.id); setMobileDetail(true); }}><item.icon size={17}/><span><b>{item.label}</b><small>{item.description}</small></span><ChevronRight size={15}/></button>)}</nav><section className={mobileDetail ? 'settings-detail' : 'settings-detail mobile-hidden'}><button className="settings-back" onClick={() => setMobileDetail(false)}><ChevronLeft size={15}/> Semua pengaturan</button><h3>{active.label}</h3><p className="muted-copy">{active.description}</p><SettingContent tab={activeTab}/></section></div></div></div> }

function SettingContent({ tab }: { tab: SettingTab }) { const { profile, setProfile } = useProfileStore(); if (tab === 'account') return <div className="setting-form"><label>Nama lengkap<input value={profile.full_name} onChange={e => setProfile({ full_name: e.target.value })}/></label><label>Username<input value={profile.username} onChange={e => setProfile({ username: e.target.value })}/></label><label>Email<input value={profile.email} onChange={e => setProfile({ email: e.target.value })} placeholder="Belum ditambahkan"/></label><label>Bio<textarea value={profile.bio} onChange={e => setProfile({ bio: e.target.value })}/></label><button className="primary-btn" onClick={() => window.dispatchEvent(new CustomEvent('sultra-toast', { detail: 'Perubahan profil tersimpan.' }))}>Simpan perubahan</button></div>; if (tab === 'privacy') return <div className="setting-rows"><ToggleRow label="Profil publik" description="Warga dapat melihat profil dan bio kamu." value={profile.profile_visibility === 'public'} onChange={value => setProfile({ profile_visibility: value ? 'public' : 'private' })}/><ToggleRow label="Status online" description="Tampilkan ketika kamu sedang aktif." value={profile.online_status_visible} onChange={value => setProfile({ online_status_visible: value })}/></div>; if (tab === 'notifications') return <div className="setting-rows"><ToggleRow label="Notifikasi push" description="Dapatkan kabar aktivitas penting." value={profile.push_notifications} onChange={value => setProfile({ push_notifications: value })}/><ToggleRow label="Email" description="Ringkasan aktivitas melalui email." value={profile.email_notifications} onChange={value => setProfile({ email_notifications: value })}/></div>; if (tab === 'appearance') return <div className="setting-rows"><ToggleRow label="Mode gelap" description="Gunakan tampilan gelap di seluruh aplikasi." value={profile.dark_mode} onChange={value => setProfile({ dark_mode: value })}/><ToggleRow label="Kurangi animasi" description="Kurangi gerakan untuk pengalaman yang lebih nyaman." value={profile.reduce_motion} onChange={value => setProfile({ reduce_motion: value })}/><label>Autoplay Reels<select value={profile.autoplay} onChange={e => setProfile({ autoplay: e.target.value as UserProfile['autoplay'] })}><option value="always">Selalu</option><option value="wifi">Hanya Wi-Fi</option><option value="never">Nonaktif</option></select></label></div>; if (tab === 'seller') return <div className="setting-empty"><Store size={28}/><h3>Mode seller</h3><p>Lengkapi verifikasi seller untuk mengelola toko, metode transaksi, dan jam operasional.</p><button className="primary-btn" onClick={() => window.dispatchEvent(new CustomEvent('sultra-toast', { detail: 'Mode seller akan tersedia setelah verifikasi.' }))}>Mulai verifikasi</button></div>; return <div className="setting-empty"><CircleHelp size={28}/><h3>Bagaimana kami dapat membantu?</h3><p>Lihat FAQ, laporkan masalah, atau kirim saran untuk SultraKita.</p><button className="soft-btn" onClick={() => window.dispatchEvent(new CustomEvent('sultra-toast', { detail: 'Pusat bantuan sedang disiapkan.' }))}>Buka pusat bantuan</button></div> }
function ToggleRow({ label, description, value, onChange }: { label: string; description: string; value: boolean; onChange: (value: boolean) => void }) { return <div className="setting-row"><div><b>{label}</b><small>{description}</small></div><button type="button" className={`toggle ${value ? 'on' : ''}`} aria-pressed={value} onClick={() => onChange(!value)}><i/></button></div> }
