'use client';

import { useEffect, useRef, useState } from 'react';
import { ArrowUp, Globe2, Image as ImageIcon, MapPin, Music2, Users, Video, X } from 'lucide-react';
import { createPost } from '@/lib/actions/posts';
import { createR2Upload } from '@/actions/upload';
import { getProfileNickname, useSessionProfile } from '@/hooks/useSessionProfile';

type Privacy = 'public' | 'followers';
type PostType = 'post' | 'reel';
type UploadedMedia = { url: string; name: string; type: string; preview: string };
type Props = { open: boolean; initialType?: PostType; onClose: () => void; onCreated?: (message: string) => void };
const MAX_CONTENT = 2000; const MAX_MEDIA = 4; const draftKey = 'suki-create-post-draft';

export function CreatePostModal({ open, initialType = 'post', onClose, onCreated }: Props) {
  const { user, profile } = useSessionProfile();
  const [textContent, setTextContent] = useState(''); const [privacy, setPrivacy] = useState<Privacy>('public'); const [location, setLocation] = useState(''); const [postType, setPostType] = useState<PostType>(initialType); const [notice, setNotice] = useState(''); const [isSaving, setIsSaving] = useState(false); const [isUploading, setIsUploading] = useState(false); const [draftRestored, setDraftRestored] = useState(false); const [media, setMedia] = useState<UploadedMedia[]>([]); const [idempotencyKey, setIdempotencyKey] = useState('');
  const fileRef = useRef<HTMLInputElement>(null); const busyRef = useRef(false);
  busyRef.current = isSaving || isUploading;
  const displayName = getProfileNickname(user, profile); const avatarUrl = profile?.avatar_url; const initials = displayName.split(/\s+/).map((part) => part[0]).join('').slice(0, 2).toUpperCase(); const trimmedContent = textContent.trim(); const canPublish = (trimmedContent.length >= 2 || media.length > 0) && !isSaving && !isUploading; const remaining = MAX_CONTENT - textContent.length;

  useEffect(() => {
    if (!open) return;
    const previousOverflow = document.body.style.overflow; document.body.style.overflow = 'hidden'; setPostType(initialType); setNotice(''); setDraftRestored(false); setIdempotencyKey(window.crypto.randomUUID());
    try { const draft = JSON.parse(window.localStorage.getItem(draftKey) || 'null'); if (draft && typeof draft === 'object' && typeof draft.content === 'string' && draft.content.trim()) { setTextContent(draft.content.slice(0, MAX_CONTENT)); setLocation(typeof draft.location === 'string' ? draft.location : ''); setPrivacy(draft.privacy === 'followers' ? 'followers' : 'public'); setDraftRestored(true); } } catch { /* optional draft */ }
    const onKeyDown = (event: KeyboardEvent) => { if (event.key === 'Escape' && !busyRef.current) onClose(); }; document.addEventListener('keydown', onKeyDown);
    return () => { document.removeEventListener('keydown', onKeyDown); document.body.style.overflow = previousOverflow; };
  }, [initialType, onClose, open]);

  async function handleFiles(event: React.ChangeEvent<HTMLInputElement>) {
    const selected = Array.from(event.target.files || []); event.target.value = ''; if (!selected.length) return;
    if (media.length + selected.length > MAX_MEDIA) { setNotice(`Maksimal ${MAX_MEDIA} media per postingan.`); return; }
    setIsUploading(true); setNotice('Mengunggah media…');
    try {
      const uploaded: UploadedMedia[] = [];
      for (const file of selected) {
        if (!file.type.startsWith('image/') && !file.type.startsWith('video/')) throw new Error('Pilih file gambar atau video.');
        const result = await createR2Upload({ fileName: file.name, contentType: file.type, size: file.size });
        const form = new FormData(); Object.entries(result.fields).forEach(([key, value]) => form.append(key, String(value))); form.append('file', file);
        const response = await fetch(result.url, { method: 'POST', body: form }); if (!response.ok) throw new Error(`Upload ${file.name} gagal (${response.status}).`);
        uploaded.push({ url: result.publicUrl, name: file.name, type: file.type, preview: URL.createObjectURL(file) });
      }
      setMedia((current) => [...current, ...uploaded]); setNotice(`${uploaded.length} media siap dipublikasikan.`);
    } catch (error) { setNotice(error instanceof Error ? error.message : 'Media gagal diunggah.'); } finally { setIsUploading(false); }
  }

  function saveDraft() { try { window.localStorage.setItem(draftKey, JSON.stringify({ content: textContent, location, privacy, savedAt: new Date().toISOString() })); setNotice('Draft tersimpan di perangkat ini.'); } catch { setNotice('Draft tidak dapat disimpan di perangkat ini.'); } }
  async function submit() {
    if (!user) { setNotice('Silakan login terlebih dahulu untuk membuat postingan.'); return; }
    if (!canPublish) { setNotice(trimmedContent.length < 2 && !media.length ? 'Tulis minimal 2 karakter atau tambahkan media.' : 'Postingan sedang diproses.'); return; }
    setIsSaving(true); setNotice('Mempublikasikan postingan…');
    const result = await createPost({ content: textContent, type: postType, privacy, location, mediaUrls: media.map((item) => item.url), idempotencyKey }); setIsSaving(false);
    if (!result.ok) { setNotice(result.error); return; }
    try { window.localStorage.removeItem(draftKey); } catch { /* optional cleanup */ } media.forEach((item) => URL.revokeObjectURL(item.preview)); setTextContent(''); setLocation(''); setMedia([]); onCreated?.(result.duplicate ? 'Postingan ini sudah ada di feed Anda.' : 'Postingan berhasil dibagikan ke feed.'); onClose();
  }

  if (!open) return null;
  const audienceLabel = privacy === 'public' ? 'Publik' : 'Pengikut'; const audienceDescription = privacy === 'public' ? 'Siapa pun dapat melihat postingan ini' : 'Hanya pengikut Anda yang dapat melihatnya';
  return <div className="fixed inset-0 z-[40] flex items-end justify-center bg-slate-950/55 p-0 backdrop-blur-[2px] sm:items-center sm:p-4" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget && !isSaving && !isUploading) onClose(); }}><section className="flex max-h-[100dvh] w-full flex-col overflow-hidden rounded-t-[24px] bg-white text-slate-900 shadow-2xl sm:max-h-[min(820px,calc(100dvh-32px))] sm:max-w-[600px] sm:rounded-[24px]" role="dialog" aria-modal="true" aria-labelledby="create-post-modal-title" aria-busy={isSaving || isUploading}><header className="flex shrink-0 items-center justify-between border-b border-slate-100 px-5 py-4"><div><p className="mb-0.5 text-[10px] font-bold uppercase tracking-[.18em] text-teal-700">SUKI COMMUNITY</p><h2 id="create-post-modal-title" className="text-xl font-extrabold tracking-tight">Buat postingan</h2></div><button type="button" onClick={onClose} disabled={isSaving || isUploading} className="grid h-10 w-10 place-items-center rounded-full bg-slate-100 text-slate-600 transition hover:bg-slate-200 disabled:opacity-50" aria-label="Tutup buat postingan"><X size={21}/></button></header><div className="min-h-0 flex-1 overflow-y-auto overscroll-contain px-5 py-5"><div className="mb-5 flex items-center gap-3"><span className="grid h-12 w-12 shrink-0 place-items-center overflow-hidden rounded-full bg-teal-700 text-sm font-extrabold text-white ring-4 ring-teal-50">{avatarUrl ? <img src={avatarUrl} alt={displayName} className="h-full w-full object-cover" /> : initials}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-extrabold">{displayName}</p><p className="mt-0.5 text-xs text-slate-500">Bagikan kabar terbaru bersama warga SultraKita</p></div><label className="relative"><span className="sr-only">Pilih audiens postingan</span><select value={privacy} onChange={(event) => setPrivacy(event.target.value as Privacy)} className="appearance-none rounded-xl border border-slate-200 bg-slate-50 py-2 pl-3 pr-7 text-xs font-bold text-slate-700 outline-none transition focus:border-teal-600 focus:ring-4 focus:ring-teal-100" aria-label="Privasi postingan"><option value="public">Publik</option><option value="followers">Pengikut</option></select><Globe2 size={13} className="pointer-events-none absolute right-2 top-1/2 -translate-y-1/2 text-teal-700" /></label></div><div className="mb-4 flex gap-2 rounded-2xl bg-slate-50 p-1.5" role="tablist" aria-label="Jenis konten"><button type="button" role="tab" aria-selected={postType === 'post'} onClick={() => setPostType('post')} className={`flex-1 rounded-xl px-3 py-2.5 text-xs font-bold transition ${postType === 'post' ? 'bg-white text-teal-800 shadow-sm' : 'text-slate-500 hover:text-slate-800'}`}>Postingan biasa</button><button type="button" role="tab" aria-selected={postType === 'reel'} onClick={() => setPostType('reel')} className={`flex-1 rounded-xl px-3 py-2.5 text-xs font-bold transition ${postType === 'reel' ? 'bg-white text-teal-800 shadow-sm' : 'text-slate-500 hover:text-slate-800'}`}>Reels</button></div>{draftRestored && <div className="mb-3 flex items-center justify-between rounded-xl border border-amber-200 bg-amber-50 px-3 py-2 text-xs text-amber-800" role="status"><span>Draft terakhir dipulihkan.</span><button type="button" className="font-bold underline" onClick={() => { setTextContent(''); setLocation(''); setDraftRestored(false); }}>Hapus</button></div>}<div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm focus-within:border-teal-500 focus-within:ring-4 focus-within:ring-teal-50"><textarea autoFocus value={textContent} onChange={(event) => setTextContent(event.target.value.slice(0, MAX_CONTENT))} placeholder={postType === 'reel' ? 'Tambahkan keterangan untuk reel Anda…' : 'Apa yang sedang Anda pikirkan? Ceritakan sesuatu yang bermanfaat untuk warga…'} maxLength={MAX_CONTENT} className="min-h-[140px] w-full resize-none border-0 bg-transparent p-0 text-[15px] leading-7 outline-none placeholder:text-slate-400 focus:ring-0" aria-label="Isi postingan" /><div className="mt-2 flex items-center justify-between border-t border-slate-100 pt-3 text-[11px] text-slate-400"><span>{remaining < 200 ? `${remaining} karakter tersisa` : 'Tulis dengan nyaman — maksimal 2.000 karakter'}</span><span>{textContent.length}/{MAX_CONTENT}</span></div></div>{media.length > 0 && <div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4" aria-label="Media terpilih">{media.map((item, index) => <div key={`${item.url}-${index}`} className="group relative overflow-hidden rounded-2xl border border-slate-200 bg-slate-100"><div className="aspect-square">{item.type.startsWith('video/') ? <video src={item.preview} className="h-full w-full object-cover" muted playsInline /> : <img src={item.preview} alt={`Pratinjau ${item.name}`} className="h-full w-full object-cover" />}</div><button type="button" onClick={() => { URL.revokeObjectURL(item.preview); setMedia((current) => current.filter((_, itemIndex) => itemIndex !== index)); }} className="absolute right-2 top-2 grid h-8 w-8 place-items-center rounded-full bg-slate-950/70 text-white" aria-label={`Hapus ${item.name}`}><X size={15}/></button><span className="absolute bottom-2 left-2 rounded-md bg-slate-950/70 px-1.5 py-1 text-[10px] text-white">{item.type.startsWith('video/') ? 'Video' : 'Foto'}</span></div>)}</div>}<div className="mt-4 rounded-2xl border border-slate-200 bg-slate-50/70 p-3"><label htmlFor="post-location" className="mb-2 flex items-center gap-2 text-xs font-bold text-slate-700"><MapPin size={15} className="text-teal-700"/> Tambahkan lokasi <span className="font-normal text-slate-400">(opsional)</span></label><input id="post-location" value={location} onChange={(event) => setLocation(event.target.value.slice(0, 120))} placeholder="Contoh: Kendari, Sulawesi Tenggara" maxLength={120} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm outline-none transition focus:border-teal-600 focus:ring-4 focus:ring-teal-100" /></div><div className="mt-4"><p className="mb-2 text-xs font-bold text-slate-600">Tambahkan ke postingan</p><div className="grid grid-cols-2 gap-2 sm:grid-cols-4"><button type="button" onClick={() => fileRef.current?.click()} disabled={isUploading || media.length >= MAX_MEDIA} className="flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-3 text-left text-xs font-bold text-teal-700 transition hover:bg-teal-50 disabled:opacity-50"><ImageIcon size={18}/> Foto / GIF</button><button type="button" onClick={() => { setPostType('reel'); fileRef.current?.click(); }} disabled={isUploading || media.length >= MAX_MEDIA} className="flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-3 text-left text-xs font-bold text-rose-600 transition hover:bg-rose-50 disabled:opacity-50"><Video size={18}/> Video / Reel</button><button type="button" onClick={() => setNotice('Tag warga akan tersedia setelah direktori warga aktif.')} className="flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-3 text-left text-xs font-bold text-indigo-600 transition hover:bg-indigo-50"><Users size={18}/> Tag warga</button><button type="button" onClick={() => setNotice('Musik dan perasaan akan tersedia pada update berikutnya.')} className="flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-3 text-left text-xs font-bold text-amber-600 transition hover:bg-amber-50"><Music2 size={18}/> Musik / Mood</button></div><input ref={fileRef} type="file" accept="image/*,video/*" multiple className="sr-only" onChange={(event) => void handleFiles(event)} /><p className="mt-2 text-[11px] text-slate-500">Maksimal 4 media. Foto 20 MB, video 50 MB. Upload memakai signed URL.</p></div>{notice && <p className="mt-4 rounded-xl bg-amber-50 px-3 py-2.5 text-xs font-medium text-amber-800" role="status">{notice}</p>}</div><footer className="shrink-0 border-t border-slate-100 bg-white px-5 pb-[calc(16px+env(safe-area-inset-bottom))] pt-4 sm:pb-4"><div className="mb-3 flex items-center gap-2 text-[11px] text-slate-500"><Globe2 size={14} className="text-teal-700"/><span><strong className="text-slate-700">{audienceLabel}</strong> · {audienceDescription}</span></div><div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><button type="button" onClick={saveDraft} disabled={isSaving || isUploading} className="rounded-xl border border-slate-200 px-4 py-3 text-xs font-bold text-slate-600 transition hover:bg-slate-50 disabled:opacity-50">Simpan Draft</button><button type="button" onClick={() => void submit()} disabled={!canPublish} className="inline-flex items-center justify-center gap-2 rounded-xl bg-teal-700 px-5 py-3 text-xs font-extrabold text-white shadow-lg shadow-teal-700/20 transition hover:bg-teal-800 disabled:cursor-not-allowed disabled:opacity-50">{isSaving ? 'Mempublikasikan…' : isUploading ? 'Mengunggah media…' : 'Publikasikan sekarang'}<ArrowUp size={15}/></button></div></footer></section></div>;
}
