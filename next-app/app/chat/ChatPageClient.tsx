'use client';
import { ChatInbox } from '@/components/chat/ChatInbox'; import { Header } from '@/components/layout/Header'; import { QuickNavBar, type QuickNavKey } from '@/components/layout/QuickNavBar'; import { SidebarMobileDrawer } from '@/components/layout/SidebarMobileDrawer'; import { useUIStore } from '@/store/ui';
export default function ChatPageClient() { const { mobileOpen } = useUIStore(); function navigate(key: QuickNavKey) { const routes: Record<QuickNavKey, string> = { home: '/beranda', campaigns: '/campaigns', groups: '/groups', market: '/jobs', suits: '/properti', marketplace: '/marketplace', referral: '/ajak-teman' }; window.location.assign(routes[key]); } return <><Header /><QuickNavBar active="home" onNavigate={navigate} /><SidebarMobileDrawer open={mobileOpen} /><main className="chat-route-main"><ChatInbox /></main></>; }
